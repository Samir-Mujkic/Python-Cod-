def python_food():
    width = 80
    text = "spam and eggs"
    left_margin = (width - len(text)) // 2

    print(" " * left_margin, text)

#def center_text(*args):
    # text = ""
     #for arg in args:
         #text += str(arg) + " "
    #left_margin = (80 - len(text)) // 2
     #print(" " * left_margin, text)

#center_text("spam and eggs")
#center_text("spam, spam, eggs")
#center_text(12)
#print()
